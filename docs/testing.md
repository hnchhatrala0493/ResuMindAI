# Testing

Run `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` from the repository root. External email, OAuth, AI, storage, and payment systems must be mocked in automated tests. Full E2E coverage is added alongside each corresponding product phase.
